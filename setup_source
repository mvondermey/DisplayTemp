sudo apt-get update
sudo apt-get install build-essential openssl libssl-dev pkg-config

cd /usr/local/src
sudo mkdir sudo
cd node
sudo wget https://nodejs.org/dist/v10.16.3/node-v10.16.3.tar.gz
sudo tar -xzf node-v10.16.3.tar.gz
cd node-v10.16.3
sudo ./configure
sudo make
sudo make install

